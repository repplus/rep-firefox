{
  "name": "rep-plus",
  "version": "1.0.0",
  "description": "Chrome DevTools extension for HTTP request manipulation",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "package": "node scripts/package.js",
    "build": "npm test && npm run package"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0"
  }
}

